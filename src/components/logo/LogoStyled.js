import styled from "styled-components";

const LogoStyled = styled.div`
  display: flex;
  align-items: center;
  cursor: pointer;

  .logoImg {
    width: 60px;
    height: 60px;
  }
`;

export default LogoStyled;
